# 真实环境验收记录

> 状态：进行中。本文只记录脱敏结果，不记录账号、Cookie、访问令牌、帖子原文或真实素材。

## 验收环境

- 日期：2026-07-25
- 服务版本：3.0.0
- 扩展协议：4
- 浏览器：Chromium 扩展模式
- HTTP 服务：本机回环地址
- 测试材料：合成素材或用户明确授权的测试内容

状态说明：

- `通过`：已在真实服务和真实浏览器链路取得可核验结果。
- `受限`：能力可运行，但受账号、页面或平台条件限制。
- `失败`：真实链路存在缺陷，需要修复后复测。
- `待确认`：下一步会产生外部副作用，需要用户现场确认。
- `未测`：尚未执行。

## 基础与会话

| 能力 | 入口或模式 | 状态 | 证据摘要 |
| --- | --- | --- | --- |
| API 健康检查 | HTTP API | 通过 | `/health` 返回正常 |
| WebUI 生产构建 | HTTP 页面 | 通过 | 静态页面可加载，服务状态已连接 |
| MCP 服务 | Streamable HTTP | 通过 | 服务已监听，未初始化请求按协议拒绝 |
| 扩展登记与心跳 | 浏览器扩展 | 通过 | 协议 4，存在一个在线实例 |
| 检查登录 | 扩展 | 通过 | 真实浏览器任务成功，返回已登录及脱敏账号存在性 |
| 二维码登录 | 扩展、API、WebUI、MCP | 通过 | 未登录分支向 WebUI 和 MCP 交付一次性图片，任务记录随后清除图片；原生页扫码确认后 WebUI、API 和 MCP 均识别为已登录 |
| 清除浏览器 Cookie | 扩展、API、WebUI、MCP | 通过 | WebUI 二次确认后扩展清理成功，API 复核浏览器登录态为否 |
| 配置 HTTP Cookie | 服务配置 | 通过 | 真实 Cookie 通过 WebUI 只写保存；接口仅返回已配置状态，重启后 HTTP 页面状态读取到登录身份 |
| 清除 HTTP Cookie | API、WebUI、MCP | 通过 | 合成值清除成功，配置恢复为未配置且无需重启 |
| 双会话账号一致性 | 扩展、HTTP | 通过 | 浏览器登录任务与 Cookie HTTP 页面均取得非空账号标识，进程内脱敏比较确认一致且不输出原值 |

## 浏览与读取

| 能力 | 入口或模式 | 状态 | 证据摘要 |
| --- | --- | --- | --- |
| 首页推荐 | 扩展 | 通过 | 读取 35 条，Schema 与访问令牌边界有效 |
| 关键词搜索 | 扩展 | 通过 | 无筛选搜索经异步结果等待后返回 40 条；筛选搜索同样返回有效结果，关键词边界正确 |
| 搜索筛选 | 扩展、MCP | 通过 | 最新、图文与一周内组合筛选真实执行成功 |
| 帖子详情 | 扩展 | 通过 | 无动态评论时正文、作者和视频信息有效；从真实搜索结果携带访问上下文直达详情成功 |
| 有界评论加载 | 扩展 | 通过 | 真实详情任务按上限读取 2 条评论 |
| 评论回复加载 | 扩展 | 受限 | 已执行包含回复的读取分支；当前两条评论未返回可核验回复 |
| 指定用户主页 | 扩展 | 通过 | 身份字段有效，真实主页读取 30 条帖子摘要 |
| 当前账号主页 | 扩展 | 通过 | 账号 ID、昵称、3 项统计和 1 条帖子均有效 |
| 长内容与分页元数据 | API、扩展 | 通过 | 真实推荐流返回 35 条、搜索返回 20 条，均通过 200 条结果上限和字段长度约束；搜索结果保留来源、关键词、布尔型 `has_more` 与字符串型 `cursor`，201 字关键词在 API 边界返回 422 |
| 短链接解析 | HTTP | 通过 | 用户授权的 `xhslink.cn` 短链接成功还原为标准作品链接，解析为图文并识别 19 个图片或动态图片资源 |
| 作品页面解析 | HTTP、SDK、CLI | 受限 | 带完整访问上下文的真实链接可解析；真实 Cookie 能确认登录身份，但私密帖子移除 `xsec_token` 后仍无作品数据，Cookie 不能替代帖子级访问上下文 |
| 图片下载 | HTTP、SDK | 通过 | 真实图文记录含 12 个 JPEG 产物，逐项具备 SHA-256 且文件校验通过 |
| 视频下载 | HTTP、SDK | 通过 | 当前 `EF4–EF7` 流分组的真实视频解析为 1 个 MP4 产物，文件大小和 SHA-256 均与接口记录一致 |
| Live 图下载 | HTTP | 通过 | 临时启用 Live 下载后，同一序号同时生成静态图片和动态图片两个产物，大小与 SHA-256 均有效；验收后已恢复原配置 |
| 缓存与指纹跳过 | HTTP、SDK | 通过 | 图文与视频均在重新解析、指纹和产物校验后跳过重复下载；新版视频流强制下载后再次请求命中缓存 |
| 断点与失败重试 | HTTP | 通过 | 真实 CDN 接受 16 KiB 后的 Range 续传，最终大小、SHA-256 与临时文件清理均通过；失败任务显式重试后复用任务标识且尝试次数递增 |

## 互动与发布

| 能力 | 状态 | 证据摘要 |
| --- | --- | --- |
| 点赞与取消点赞 | 通过 | 可信鼠标输入启用点赞后，新页面回读 `active=true / changed=false`；取消后再次跨页面回读 `active=false / changed=false`，最终恢复未点赞 |
| 收藏与取消收藏 | 通过 | 可信鼠标输入启用收藏后，新页面回读 `active=true / changed=false`；取消后再次跨页面回读 `active=false / changed=false`，最终恢复未收藏 |
| 发表评论 | 受限 | 输入、按钮启用和点击均真实执行；扩展与真实浏览器输入都会触发平台扫码验证，尚无评论落库 |
| 回复评论 | 未测 | 测试评论未落库，禁止创建无效或重复回复 |
| 图文立即发布 | 通过 | 合成图文完成素材、文案、标签和私密范围设置；CDP 鼠标未生效时，受控 DOM 兜底确认按钮仍可用后提交，平台自动回传成功 |
| 视频立即发布 | 通过 | 5 秒 H.264 合成视频完成上传、处理、文案与私密范围设置；CDP 真实鼠标点击后平台确认成功，任务直接进入 `published` |
| 原创声明 | 通过 | 全合成图文冻结为 `is_original=true`、仅自己可见且无商品；扩展处理原创须知并回读开关开启，任务以一次尝试获平台确认发布成功 |
| 可见范围 | 通过 | 图文任务设置并回读为“仅自己可见”，最终发布包同样冻结为 `private` |
| 商品绑定 | 受限 | 用户已明确授权绑定列表第一项；私密、非原创、全合成图文真实进入发布编辑页，但当前账号没有“添加商品”入口，也没有可读取的商品列表，任务在点击发布前安全失败；唯一匹配、歧义拒绝和缺失入口分支由扩展测试覆盖 |
| 本地定时发布 | 通过 | 私密图文按计划从 `scheduled` 自动转为 `ready`，扩展闹钟主动领取、打开创作页、填充并提交；全程无人工操作，任务以一次尝试进入 `published` |
| 官方定时发布 | 通过 | 私密合成图文以一次尝试提交；平台接口回读 `post_timing=true`，`schedule_post_time` 对应任务计划的 UTC 分钟，可见范围仍为“仅自己可见” |
| 人工核对与重试 | 通过 | 发布中租约过期进入待确认，禁止直接重试；人工确认未发布后可重试 |

## 入口与恢复

| 能力 | 状态 | 证据摘要 |
| --- | --- | --- |
| WebUI 调用链 | 通过 | 真实帖子库可恢复；无效链接的 API 错误会显示在页面通知中 |
| MCP 工具调用链 | 通过 | 真实连接列出 16 个工具；二维码未登录分支返回图片内容，已登录分支只返回文本，推荐流返回 35 条 |
| CLI 解析与下载 | 通过 | `detail` 输出有效中文 JSON；`download` 重新校验真实产物并命中指纹缓存 |
| Python SDK | 通过 | `detail` 真实解析成功；`download` 对图文、视频记录完成哈希校验并命中缓存 |
| 扩展自动、浏览器、后台下载模式 | 通过 | 授权帖子中仅选择 1 项媒体；自动模式在服务在线时提交后台任务并命中有效产物，浏览器模式真实生成 JPEG 文件 |
| 扩展记录同步 | 通过 | 浏览器下载产生 1 条本地待同步记录，面板同步后服务端保存正式记录；合成重复记录仍只保存一条 |
| API 重启恢复 | 通过 | 合成排队任务在服务重启后自动执行，命中缓存并完成 |
| 扩展离线与恢复 | 通过 | API 停止后面板切换为独立模式，自动选择真实降级为浏览器下载；API 恢复后扩展重新在线并同步 3 条待同步记录 |
| 幂等请求 | 通过 | 下载与浏览器任务重复提交均复用同一任务；冲突参数返回 400 |
| 租约过期 | 通过 | 只读任务过期后重新排队，已运行写任务不自动重试 |
| `needs_review` | 通过 | 已运行写任务租约过期后进入待确认，直接重试被拒绝 |
| 页面兼容性诊断 | 通过 | 真实二维码适配失败曾返回页面类型、适配器版本以及命中和缺失锚点，修复后任务恢复成功 |
| 脱敏失败截图 | 通过 | 真实失败任务生成本机脱敏截图，服务端只接收本地产物标识，不传输截图或页面原文 |

## 已确认缺陷

| 编号 | 严重度 | 缺陷 | 状态 | 当前证据 |
| --- | --- | --- | --- | --- |
| A-01 | 高 | 扩展读取 Vue 实时状态时直接跨桥传递响应式对象，评论详情出现循环引用并无法序列化 | 已通过 | 真实复测未再出现循环引用 |
| A-02 | 高 | 搜索筛选依赖已经失效的固定 DOM 选择器 | 已通过 | 参考原项目的悬停浮层机制后，真实组合筛选返回 60 条结果 |
| A-03 | 高 | 当前账号主页的站内二次导航没有继续完成任务 | 已通过 | 真实点击导航成功，账号 ID、昵称、统计和帖子均有效 |
| A-05 | 中 | 扩展登记表的启动迁移存在并发竞争 | 已通过 | 服务重启及扩展并发轮询未再出现迁移错误 |
| A-06 | 高 | 应用日志会输出真实作品 ID、标题、作者或产物路径 | 已通过 | 真实缓存解析日志未出现作品、作者、链接或产物路径 |
| A-07 | 低 | 已登录时二维码 API 的任务消息仍声称已经生成二维码 | 已通过 | 真实返回“浏览器已登录，无需再次扫码” |
| A-08 | 高 | 详情页任务早于评论容器异步渲染完成 | 已通过 | 真实任务成功读取 2 条评论 |
| A-09 | 高 | 登录容器先于二维码图片异步渲染，扩展过早读取并误报失败 | 已通过 | 增加有界等待后，WebUI 与 MCP 均真实取得二维码图片 |
| A-10 | 中 | 二维码任务在后台标签运行，扫码握手不能稳定落地 | 已修复 | 原生前台页面扫码登录真实通过；二维码任务现改为前台打开并保留原生页面 |
| A-11 | 高 | 收藏控件实际为 SVG，直接调用 `HTMLElement.click()` 会失败 | 已通过 | SVG 控件定位与回归测试通过，正式写入改由浏览器级可信鼠标输入完成 |
| A-12 | 高 | 评论输入框异步渲染且直接修改文本不能稳定启用提交按钮 | 已通过 | 原生编辑命令、有界等待和延迟渲染测试通过，真实页面可启用并点击发送 |
| A-13 | 高 | 互动与发布依赖内容脚本合成事件，且安全验证后不能保留原任务和页面恢复 | 部分通过 | 收藏已迁移到受控可信输入并跨页面持久化；评论扫码后的原任务保留与恢复仍待实现 |
| A-14 | 高 | 发布按钮位于新版封闭影子根，聚焦后发送 CDP 回车没有触发提交 | 已通过 | 改为桥接按钮坐标并发送 CDP 真实鼠标点击后，视频任务由 `publishing` 直接进入 `published` |
| A-15 | 中 | 把 `manual` 任务没有被后台领取误判为发布闹钟失效 | 已澄清 | Service Worker 中两个闹钟均每 30 秒触发，能力探测和领取请求返回 200；`manual` 必须由带任务标识的页面明确领取，后台只主动领取 `scheduled` |
| A-16 | 高 | 图片页发布按钮聚焦时发生滚动，桥接层却返回滚动前坐标 | 已通过 | 保留 CDP 鼠标为首选，并在确认页面仍停留且按钮仍可用时调用封闭影子根内的真实按钮作为防重复兜底；私密定时图文全程无人工操作并由平台确认发布成功 |
| A-17 | 高 | 官方定时控件曾按浏览器所在时区格式化绝对时间；非中国时区会向平台写入过去时间并退化为立即发布 | 已通过 | 日期固定按 `Asia/Shanghai` 格式化，并通过浏览器级可信输入写入；最终平台回读 `post_timing=true` 且非零 `schedule_post_time`，计划分钟与任务一致 |
| A-18 | 高 | 互动任务可能早于控件挂载，且后台标签中的 CDP 鼠标输入不会被页面接受 | 已通过 | 增加互动控件有界等待，点击前临时将任务页置前并在完成后恢复原标签；点赞、收藏及各自取消操作均通过跨页面复核 |
| A-19 | 中 | 无筛选搜索会在平台异步写入结果前读取状态，任务成功但返回空列表 | 已通过 | 对搜索主世界状态增加有界等待；同一普通关键词真实复测由 0 条恢复为 40 条，并可继续打开帖子详情 |
| A-20 | 高 | HTTP 视频解析器只枚举历史 `h264/h265` 流分组，平台改用 `EF4–EF7` 后会返回视频类型但媒体为空 | 已通过 | 改为遍历所有合法流分组并沿用分辨率、码率或大小排序；真实视频产出 1 个 MP4，文件大小和 SHA-256 复核通过 |
| A-21 | 中 | 原创声明必须处理平台须知确认窗口，并在发布前核验最终开关状态 | 已通过 | 全合成私密图文开启原创声明后一次发布成功；发布包、确认流程和开关回读均为开启状态 |

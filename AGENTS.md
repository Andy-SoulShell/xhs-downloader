# 项目偏好

## 1. 通用

- 中文优先，文件统一使用 UTF-8；CLI、文档、错误提示、UI 文案等用户可见内容须使用自然简体中文。技术标识符使用英文；命名、注释与 docstring 须准确、通顺、简洁，做到“信达雅”，避免生硬直译和无意义注释。
- 用户限定目录时，只修改该目录及其子目录，不顺手改动范围外的代码、数据、脚本、测试或生成产物。
- Git 提交按语义拆分；只暂存本任务文件，提交前后复核 `git status` 与暂存差异。边开发边运行与改动范围相称的验证，每项独立能力验证通过后分批提交。
- 手写代码单文件不得超过 300 行，并由自动化测试约束；达到上限时必须按职责拆分，禁止通过白名单、压缩排版或降低可读性绕过。可重复生成且由生成命令校验的代码不受此限，也禁止手工拆改。

## 2. 架构与 Python

- 保持清晰分层和克制的目录深度，CLI 只负责交互，领域、应用、模型与基础设施逻辑分离；坚持单一职责、短函数和显式类型边界。
- 有状态、有生命周期或存在可替换实现的能力使用类与 `Protocol`；无状态转换和纯算法使用函数。按需采用 Strategy、Repository、Factory，禁止上帝类和为套用模式制造空壳抽象。
- Python 模块及公开类、函数、方法必须使用清晰的中文 Google 风格 docstring；实例方法、`classmethod`、`staticmethod`、属性、`Protocol` 方法和抽象方法均属公开接口。
- Docstring 根据签名提供 `Args`、`Returns`、`Raises`；`__init__` 不写无意义的 `Returns: None`。类说明职责、协作边界、关键状态及必要的 `Attributes`；复杂私有逻辑说明隐含约束，简单局部纯函数可省略重复说明。
- 遵循 Python、uv、Ruff、Pydantic、pydantic-settings、Typer、FastAPI、FastMCP、React、TypeScript、Tailwind CSS 与 Radix UI 的主流最佳实践。

## 3. 前端分层与注释

- 前端遵循“Radix 原语 → 可复用基础组件 → 业务组件 → 页面或工作台”的依赖方向；页面只负责组合，不直接承载复杂数据转换、排序筛选或状态算法。
- 可独立测试的前端逻辑必须从 React 组件中提取；数据模型、请求逻辑与展示组件分离。
- 重复的卡片、控制栏、状态提示等应抽成有业务语义的组件，但不创建无语义、无复用价值的碎片组件。
- TypeScript 遵循“类型即文档”：注释只说明类型无法表达的原因、不变量、单位、副作用、默认值和弃用信息，不复述类型、函数签名或 JSX。
- 新增或修改的导出组件、Hook、工具函数与类型应写简短中文 JSDoc；内部一次性辅助函数按需说明。
- `useEffect` 应说明为什么存在以及同步哪个外部系统；取消标志、版本号、`ref` 等异步守卫应说明所防止的陈旧运行或竞态。
- 优先通过准确命名表达意图；算法参数和调优值抽为具名常量，不保留注释掉的代码。

## 4. 设计系统与布局

- 组件新增或修改视觉值时，字体、颜色、阴影、圆角和尺寸优先使用语义化 Tailwind Token；禁止继续引入无语义的硬编码色值和魔法数。
- 页面尺寸由 Grid、Flex、`minmax()` 和断点决定；可收缩子项按需设置 `min-w-0`、`min-h-0`。
- 每个长内容区应有明确且唯一的滚动容器，不得使用 `overflow-hidden` 掩盖横向溢出、内容裁切或滚动职责不清。

## 5. 受管浏览器

- 无头运行时必须覆盖 User-Agent，让小红书看到的是一个正常浏览器；版本号问本机浏览器自己要，不得写死。默认 UA 里的 `HeadlessChrome` 标记会让登录页连二维码都不渲染——实测只有 UA 这一项决定成败，窗口大小与 `navigator.webdriver` 都不影响。
- 受管浏览器的窗口显示方式（不显示窗口、窗口移出屏幕、正常显示）由设置决定，不得写死；任何一种方式下读取、互动与扫码登录都必须可用。
- 页面适配器只读当前页面，不主动导航；改动选择器或超时前先用真实页面量一遍，不靠猜。

## 6. 数据、安全与测试材料

- 解析、下载与发布阶段须使用结构化类型边界、内容指纹缓存和可恢复产物；上游输入或指纹变化时结果立即失效，不得静默复用过期数据。
- Mock 数据必须符合正式 Schema，并明确标记为“模拟数据”或“Demo”，不得冒充真实解析、下载或发布结果。
- `.env.example` 须按功能分组，并逐项说明用途、类型、单位、可选值、默认行为与安全注意事项。
- 公开测试、示例和文档只能使用完全合成、公版或已获明确授权的材料。
- 不得提交真实小红书内容、Cookie、发布素材、下载产物、API Key、`.env`，或包含敏感 URL、请求头、请求正文和用户原文的日志。

## 7. 验收

- Python 改动至少运行与范围相称的 Ruff 检查、格式检查和 Pytest；不得仅以进程退出码代替结果核对。
- WebUI 改动至少运行 `pnpm lint`、`pnpm test` 和生产构建；浏览器扩展改动至少运行 TypeScript 检查、测试和生产构建。
- 布局或交互改动至少验证典型与窄屏宽度、长文本、空状态、错误状态、滚动到底、无横向滚动和无裁切遮挡；必须实际操作核心交互，不能只看截图。
- 静态构建须通过 HTTP 服务器验收，不得使用 `file://`；部署须保证正确 MIME、SPA 回退与缓存刷新策略。
- 已有仓库级失败须提供证据并说明与本任务的关系；不得通过跳过测试、修改真值、降低断言或放宽质量门槛消除失败。
- 失败原因对外可以脱敏，但必须在本机日志里留下原文；只留一句通用失败会让问题无从查起。

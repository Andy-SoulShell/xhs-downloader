# 项目偏好

- 中文优先，文件统一使用 UTF-8；CLI、文档、错误提示等用户可见内容须使用自然简体中文。命名、注释与 docstring 须准确、通顺、简洁，做到“信达雅”，避免生硬直译和无意义注释。
- 保持清晰分层和克制的目录深度，CLI 只负责交互，领域、应用、模型与基础设施逻辑分离。有状态、有生命周期或可替换的能力使用类与 `Protocol`，无状态转换和纯算法使用函数；按需采用 Strategy、Repository、Factory，禁止上帝类和空壳抽象。
- 单个代码文件不得超过 300 行，并由自动化测试约束。Python 模块及公开类、函数、方法必须使用清晰的中文 Google 风格 docstring；实例方法、`classmethod`、`staticmethod`、属性、`Protocol` 方法和抽象方法均属公开接口。根据签名提供 `Args`、`Returns`、`Raises`，`__init__` 不写无意义的 `Returns: None`；类说明职责、协作边界、关键状态及必要的 `Attributes`，复杂私有逻辑说明隐含约束，简单局部纯函数可省略重复说明。
- 采用 uv、Python、Ruff、Pydantic、Typer、Agno 和 DeepSeek 的主流最佳实践；边开发边运行与改动范围相称的验证，每项独立能力通过后分批提交。
- 模型阶段须使用结构化类型边界、内容指纹缓存和可恢复产物，不得静默复用过期结果。
- `.env.example` 须按功能分组，并逐项说明用途、类型、单位、可选值、默认行为与安全注意事项。
- 公开测试、示例和文档只能使用完全合成、公版或已获明确授权的材料；不得提交 `screenplay/` 中的真实剧本、模型分析产物、API Key、`.env` 或含敏感原文的日志。
